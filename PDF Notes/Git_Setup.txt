step 1:open gitBash
git init
git config --list


Step 2:First time config:
git config --global user.name "srikanth138"
git config --global user.email "srikanthp825@gmail.com"
git config --list

step 3: 
git add "singlefileName"/ --a(All files)
git commit -m "msg"
git push -origin<repository name> (first time)/git push repositoryName

Step 4:
git pull origin master (the key)
At last git push origin master (solve all problem).

Step 5:
git --help
git status
git branch
git checkout

git config --global init.defaultBranch main


